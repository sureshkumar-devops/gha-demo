name: Hello World
on: 
  push:
    branches:
      - master
      - dev
      - stage
      - 'release/**'

  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Phase
        run: echo "A demo build"

    
   
  
