name: Hello World
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Phase
        run: |
           echo "Hello, ${{ vars.MY_NAME }}"
           echo "Hello, ${{ secrets.DOCKER_PASSWORD }}"

    
   
  
